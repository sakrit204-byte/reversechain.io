packages:
  - 'apps/*'
  - 'packages/*'
  - 'services/*'
  # The WordPress theme has a front-end build (TypeScript, Three.js) even
  # though the theme itself is PHP. Including it here keeps one lockfile and
  # one install for the whole repository.
  - 'wordpress/themes/*'
